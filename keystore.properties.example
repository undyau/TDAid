# Copy to keystore.properties (gitignored) and fill in real values.
# Never commit the real keystore.properties or the .jks file.
storeFile=C:/path/to/tdaid-release.jks
storePassword=changeme
keyAlias=tdaid
keyPassword=changeme
